- clean: ['~']

- link:
    ~/.profile: profile
    ~/.bashrc: bashrc
    ~/.bash_logout: bash_logout
    ~/.zshrc: zshrc
    ~/.antigen:
    ~/.antigen.zsh:
    ~/.p10k.zsh:

- create:
    - ~/workspace

- shell:
  - chsh -s $(which zsh)
